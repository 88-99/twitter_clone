<h1>リスト</h1>
<p><%= notice %></p>

<table>
<% @posts.each do |post| %>
  <tr>
    <td><%= post.content %></td>
    <td><%= link_to 'このつぶやきへGO!', post_path(post.id) %></td>
  </tr>
<% end %>
</table>
<%= link_to 'つぶやく',  new_post_path %>
